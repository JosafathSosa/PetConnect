import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  content: {
    backgroundColor: "white",
    flex: 1,
  },

  btnContainer: {
    position: "absolute",
    right: 10,
    bottom: 10,
  },
});
